package server_api

/*
func GetAlbum(enc Encoder) (int, string) {
  // db DB, parms martini.Params

  // id, err := strconv.Atoi(parms["id"])
  // al := db.Get(id)
  // if err != nil || al == nil {
  //   return http.StatusNotFound, Must(enc.Encode(
  //     NewError(ErrCodeNotExist, fmt.Sprintf("the album with id %s does not exist", parms["id"]))))
  // }

  al := structures.User{FirstName: "Write presentation", InstaUserID: 1234567890}

  return http.StatusOK, enc.Encode(al)
}
*/
